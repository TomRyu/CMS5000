<UserControl x:Class="CMS5000.Controls.LightningChartSampleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lcusb="http://schemas.lightningchart.com/chartingMVVM/"
             x:Name="Root">
    <UserControl.Resources>
        <Style x:Key="DimBtnStyle" TargetType="Button">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                CornerRadius="3" Padding="4,2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.82"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.65"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <lcusb:LightningChart x:Name="XyChart"      ChartName="XY sample"/>
        <lcusb:LightningChart x:Name="PolarChart"   ActiveView="ViewPolar" ChartName="Polar sample"   Visibility="Collapsed"/>
        <lcusb:LightningChart x:Name="SurfaceChart" ActiveView="View3D"    ChartName="Surface sample" Visibility="Collapsed"/>

        <!-- 2D / 3D 토글 — Waterfall·Cascade에서만 표시 -->
        <StackPanel x:Name="DimToggle"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Margin="0,8,10,0" Panel.ZIndex="10"
                    Visibility="Collapsed">
            <Border Background="#BB101020" BorderBrush="#3A3A52"
                    BorderThickness="1" CornerRadius="5" Padding="3,3">
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="Btn2D" Content="2D" Click="OnToggle2D"
                            Style="{StaticResource DimBtnStyle}"
                            Background="#2979FF" Foreground="White"/>
                    <Button x:Name="Btn3D" Content="3D" Click="OnToggle3D"
                            Style="{StaticResource DimBtnStyle}"
                            Background="Transparent" Foreground="#888888"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
