<Window x:Class="CMS5000.Views.Admin.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="비밀번호 확인"
        Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False"
        Background="#F0F0F3">
    <Window.Resources>
        <Style x:Key="Lbl" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,8,0,3"/>
        </Style>
        <Style x:Key="Fld" TargetType="PasswordBox">
            <Setter Property="Height" Value="28"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="6,0"/>
        </Style>
        <Style x:Key="Btn" TargetType="Button">
            <Setter Property="Padding" Value="22,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="#FFFFFF" CornerRadius="4" BorderBrush="#9AA7BD" BorderThickness="1" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="bg" Property="Background" Value="#EAF1FB"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <StackPanel Margin="18">
        <Border Background="#FFF7E6" BorderBrush="#F0C36D" BorderThickness="1" CornerRadius="4" Padding="10,8" Margin="0,0,0,4">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="&#xE72E;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="#C2660C" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock x:Name="PromptText" TextWrapping="Wrap" FontSize="12" Foreground="#7A4A00" VerticalAlignment="Center" Width="320"/>
            </StackPanel>
        </Border>

        <TextBlock Text="비밀번호" Style="{StaticResource Lbl}"/>
        <PasswordBox x:Name="PwBox" Style="{StaticResource Fld}"/>

        <StackPanel x:Name="ConfirmRow" Visibility="Collapsed">
            <TextBlock Text="비밀번호 확인" Style="{StaticResource Lbl}"/>
            <PasswordBox x:Name="PwConfirmBox" Style="{StaticResource Fld}"/>
        </StackPanel>

        <TextBlock x:Name="StatusText" FontSize="11" Foreground="#C0392B" Margin="0,8,0,0" TextWrapping="Wrap"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button x:Name="OkButton" Style="{StaticResource Btn}" Margin="0,0,10,0" IsDefault="True" Click="Ok_Click">
                <TextBlock Text="확인" FontSize="12" FontWeight="SemiBold" Foreground="#1565C0"/>
            </Button>
            <Button Style="{StaticResource Btn}" IsCancel="True" Click="Cancel_Click">
                <TextBlock Text="취소" FontSize="12" FontWeight="SemiBold" Foreground="#C0392B"/>
            </Button>
        </StackPanel>
    </StackPanel>
</Window>
